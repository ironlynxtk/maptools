import * as React from 'react';

// eslint-disable-next-line @typescript-eslint/no-empty-interface
export interface ZonesProps {}

const Zones: React.SFC<ZonesProps> = () => {
  return <h1>Zones</h1>;
};

export default Zones;
